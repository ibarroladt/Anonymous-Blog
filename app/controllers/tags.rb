#GETS========================================================================

get '/tag/:id' do
  id = params[:id]
  @tag = Tag.find(id)
  erb :tag
end

#POST========================================================================